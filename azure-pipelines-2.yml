trigger:
  - master

pool:
  name: Default

steps:
  - script: echo "Hello, it's my Azure DevOps App"
    displayName: 'Run an echo command'

  - script: mvn clean install
    displayName: 'Build with Maven'

  - script: |
      echo -e "10\n5\n+" | java -jar target/SimpleCalci-0.0.1-SNAPSHOT.jar
    displayName: 'Run JAR file of SimpleCalci App'
